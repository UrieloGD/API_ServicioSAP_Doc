# Mapeo del Método: `GET /customerService/bbvaKeyAdvanced` — Capa LAN (Controller)

**Archivo:** `APIMagento/WebApiMagento/Controllers/CustomerServiceController.cs`
**Método:** `GetBBVAKeyAdvanced()` — Líneas 158–164
**Capa:** LAN (Nexo)
**Rol en el flujo:** Dispatcher.

---

## Flujo de Ejecución

1. Recibe petición `GET`.
2. Llama a `CustomerServiceMethods.GetBBVAKeyAdvanced()`.
3. Retorna `Ok(response)`.

## Interacciones con Base de Datos

**Ninguna directa.**
