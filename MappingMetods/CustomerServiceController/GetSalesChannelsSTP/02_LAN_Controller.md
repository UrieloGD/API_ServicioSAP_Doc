# Mapeo del Método: `POST /customerService/GetSalesChannelsSTP` — Capa LAN (Controller)

**Archivo:** `APIMagento/WebApiMagento/Controllers/CustomerServiceController.cs`
**Método:** `GetSalesChannelsSTP(GetSalesChannelsSTPRequest request)` — Líneas 195–199
**Capa:** LAN (Nexo)
**Rol en el flujo:** Dispatcher.

---

## Flujo de Ejecución

1. Recibe petición `POST`.
2. Llama a `CustomerServiceMethods.GetSalesChannelsSTP(request)`.
3. Retorna envuelto en `Json(...)`.

## Interacciones con Base de Datos

**Ninguna directa.**
