# Mapeo del Método: `GET /customerService/bbvaKeyNeko` — Capa LAN (Controller)

**Archivo:** `APIMagento/WebApiMagento/Controllers/CustomerServiceController.cs`
**Método:** `GetBBVAKeyNeko()` — Líneas 142–148
**Capa:** LAN (Nexo)
**Rol en el flujo:** Dispatcher.

---

## Flujo de Ejecución

1. Recibe petición `GET`.
2. Llama a `CustomerServiceMethods.GetBBVAKeyNeko()`.
3. Retorna `Ok(response)`.

## Interacciones con Base de Datos

**Ninguna directa.**
